# Blog API

Добро пожаловать в **Blog API** — это backend-приложение, реализованное на **FastAPI** и **SQLAlchemy**, с поддержкой асинхронной работы с базой данных PostgreSQL. Проект включает в себя полный CRUD для пользователей, постов и комментариев, а также систему подписок.

---

## ✨ Особенности

- 🚀 **FastAPI** — современный, быстрый (высокая производительность) веб-фреймворк для создания API с поддержкой асинхронности.
- 🐍 **Python 3.13** — используется одна из последних версий Python.
- 🗄️ **PostgreSQL** — надёжная реляционная база данных.
- 🔄 **Асинхронность** — все операции с базой данных асинхронны (`asyncpg`, `AsyncSession`).
- 🧩 **Alembic** — управление миграциями базы данных.
- 🐳 **Docker & Docker Compose** — контейнеризация приложения и базы данных.
- 📦 **Pydantic** — валидация данных на входе и выходе из API.

## 🚀 Запуск проекта

Следуйте этим шагам, чтобы запустить проект локально или в контейнере с Docker.

### 1. Клонируйте репозиторий

```bash

git clone https://github.com/panfiloow/blog_api.git
cd blog_api

```

### 2. Установите зависимости (если запускаете без Docker)
Убедитесь, что у вас установлен Python 3.13.
```bash
pip install -r requirements.txt
```

### 3. Настройте переменные окружения
Создайте файл .env в корне проекта на основе шаблона .env.example:

```bash
cp .env.example .env
```

### 4. Запуск с Docker Compose (рекомендуется)
Этот способ автоматически запустит и PostgreSQL, и приложение в контейнерах.

Убедитесь, что у вас установлен Docker и Docker Compose .

```bash
docker-compose up --build
```

Приложение будет доступно по адресу: http://localhost:8000

Документация к API: http://localhost:8000/docs

### 5. (Опционально) Запуск без Docker
Если вы не используете Docker, убедитесь, что у вас запущен PostgreSQL на указанном в .env хосте и порту.

Затем выполните:

```bash
# Примените миграции Alembic (если нужно)
alembic upgrade head

# Запустите приложение
uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
```

## 🛠️ Управление миграциями

Проект использует **Alembic** для отслеживания изменений в структуре базы данных и применения миграций.

### Создание новой миграции

Когда вы вносите изменения в модели (например, добавляете новое поле), выполните следующую команду, чтобы создать файл миграции:

```bash
alembic revision --autogenerate -m "описание миграции"
```

Применение миграций
Чтобы применить все неприменённые миграции к базе данных:

```bash
alembic upgrade head
```

Откат последней миграции
Если нужно откатить последнюю миграцию:

```bash
alembic downgrade -1
```

Проверка статуса миграций
Посмотреть, какие миграции уже применены:
```bash
alembic current
```

При запуске через Docker Compose, миграции автоматически применяются при старте контейнера fastapi_app. 

## 📡 API Эндпоинты

### Пользователи (`/users`)

- `POST /users/` — регистрация пользователя.
- `GET /users/{id}` — получить пользователя по ID.
- `GET /users/` — получить список пользователей (с пагинацией).
- `PUT /users/{id}` — обновить данные пользователя.
- `DELETE /users/{id}` — удалить пользователя (каскадно удаляются посты и комментарии).
- `POST /users/{id}/follow` — подписаться на пользователя.
- `DELETE /users/{id}/unfollow` — отписаться от пользователя.
- `GET /users/{id}/followers` — получить список подписчиков.
- `GET /users/{id}/following` — получить список, на кого подписан пользователь.

### Посты (`/posts`)

- `POST /posts/` — создать пост (требует `title`, `content`, `author_id`).
- `GET /posts/{id}` — получить пост по ID.
- `GET /posts/` — получить список постов (с пагинацией, фильтрацией по автору, дате).
- `PUT /posts/{id}` — обновить пост.
- `DELETE /posts/{id}` — удалить пост.

### Комментарии (`/comments`)

- `POST /comments/` — создать комментарий (требует `content`, `author_id`, `post_id`).
- `GET /comments/{id}` — получить комментарий по ID.
- `GET /comments/` — получить список комментариев (с пагинацией, фильтрацией по посту или автору).
- `PUT /comments/{id}` — обновить комментарий.
- `DELETE /comments/{id}` — удалить комментарий.


## 👤 Автор

**Илья Панфилов**  
GitHub: [panfiloow](https://github.com/panfiloow)

Проект **Blog API** был создан в рамках обучения и практики разработки на **FastAPI**, **SQLAlchemy** и **PostgreSQL**.
